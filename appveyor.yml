environment:
  matrix:
    - nodejs_version: "10"
    - nodejs_version: "12"
    - nodejs_version: "15"
install:
  - ps: Install-Product node $env:nodejs_version
  - yarn install
build: off
test_script:
  - node --version
  - npm --version
  - yarn --version
  - yarn run lib
